/** G Force Energy Ops Skill — placeholder for ERCOT/PowerConnection integration. */
async function run(action, params, context) {
  return `Energy ops skill: action='${action}'. Full ERCOT integration in Phase 2. Set ERCOT_API_KEY and PC_API_KEY in .env.`;
}
module.exports = { run };
